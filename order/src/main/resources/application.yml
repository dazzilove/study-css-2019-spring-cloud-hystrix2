spring:
  application:
    name: order

server:
  port: 2001

management:
  endpoints:
    web:
      exposure:
        include: '*'


eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:9002/eureka/